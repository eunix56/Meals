<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools">

    <TextView
        android:id="@+id/tv_instructions_num"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        app:layout_constraintTop_toTopOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        android:layout_marginTop="@dimen/normal_spacing"
        android:layout_marginStart="@dimen/normal_spacing"
        android:background="@drawable/bg_instructions_num"
        android:theme="@style/NumberingTextView"
        android:paddingTop="3dp"
        android:paddingBottom="3dp"
        android:paddingStart="9dp"
        android:paddingEnd="9dp"
        tools:text="1"
        />

    <TextView
        android:id="@+id/tv_instruction"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        app:layout_constraintTop_toTopOf="@id/tv_instructions_num"
        app:layout_constraintStart_toEndOf="@id/tv_instructions_num"
        app:layout_constraintEnd_toEndOf="parent"
        android:layout_marginStart="@dimen/normal_spacing"
        android:layout_marginEnd="@dimen/normal_spacing"
        app:layout_constraintBottom_toBottomOf="parent"
        android:theme="@style/CardGreyTextView"
        android:textColor="@color/text_colour"
        tools:text="Boil water and add all the necessary ingredients"
        />

</androidx.constraintlayout.widget.ConstraintLayout>